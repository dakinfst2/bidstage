{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "releaseCommand": "FLASK_APP=app.py flask db-upgrade-custom",
    "startCommand": "gunicorn app:app --bind 0.0.0.0:$PORT"
  }
}
